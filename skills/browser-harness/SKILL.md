---
name: browser-harness
description: Direct browser control via CDP. Use for web automation, scraping, or site interaction.
---

Provide a `task` description. The harness will attempt to complete it using self-healing CDP scripts.
Required parameter: { "task": "string" }
Optional parameter: { "code": "string (custom CDP script)" }
